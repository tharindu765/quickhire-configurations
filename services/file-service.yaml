server:
  port: 8083

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

storage:
  provider: local
  local:
    base-path: uploads
    base-url: http://localhost:8083/files

