server:
  port: 0

spring:
  mvc:
    problemdetails:
      enabled: true
  mongodb:
    host: localhost
    port: 13500
    database: quickhire_jobs
    username: root
    password: mongodb
    authentication-database: admin

eureka:
  instance:
    instanceId: ${spring.application.name}:${vcap.application.instance_id:${spring.application.instance_id:${random.value}}}
